# Pycord (no discord.py): aporta discord.sinks y voice_client.start_recording.
# Instalado desde la rama master porque las versiones publicadas no sirven:
#   - 2.7.x no soporta DAVE/E2EE -> no conecta a voz (error 4017) tras marzo 2026.
#   - 2.8.0 conecta con DAVE pero rompe la API clásica de grabación (sink_listeners).
# master junta ambas cosas (DAVE + grabación clásica). Es código no publicado:
# cuando py-cord saque una versión estable con DAVE, fijar esa versión aquí.
py-cord[voice] @ git+https://github.com/Pycord-Development/pycord.git@master
gspread
oauth2client
python-dotenv
PyNaCl
pydub
ffmpeg-python
openai
mistralai
# Transcripción local con Whisper (proveedor 'whisper'): opcional, instala PyTorch.
# No se incluye en la imagen Docker ligera. Para usarlo:
#   pip install -r requirements-whisper.txt
# openai-whisper
