# Pycord (no discord.py): librería de Discord con soporte de voz.
# La grabación EN VIVO está deshabilitada por flag (LIVE_RECORDING_ENABLED) porque
# Discord forzó cifrado E2EE/DAVE en marzo 2026 y py-cord aún no descifra el audio
# recibido (issue #3139). 2.8.0 sí soporta el handshake DAVE para conectar a voz.
# Cuando py-cord publique el fix de recepción, fijar esa versión y activar el flag.
py-cord[voice]==2.8.0
gspread
oauth2client
python-dotenv
PyNaCl
pydub
ffmpeg-python
openai
mistralai
# Transcripción local con Whisper (proveedor 'whisper'): opcional, instala PyTorch.
# No se incluye en la imagen Docker ligera. Para usarlo:
#   pip install -r requirements-whisper.txt
# openai-whisper
